# Copy to .env for local standalone use: `cp .env.example .env && docker compose up -d`
# Coolify supplies these at deploy time — .env is never committed.

# The URL users reach the app on. Backend and frontend build absolute links from
# it, so a wrong value gives a working login page that redirects to the wrong host.
CISO_ASSISTANT_URL=https://localhost:8443

# Django ALLOWED_HOSTS. Anything absent from this list gets a 400
# "Invalid HTTP_HOST header", and three callers need to be in it: users on the
# public hostname, the backend healthcheck (Host: localhost) and the frontend's
# server-side API calls (Host: backend).
#
# On Coolify, set the WHOLE list in the environment-variable UI —
# `<public hostname>,backend,localhost`. Coolify supplies this variable from its
# own store and the `,backend,localhost` in the compose file does not reach the
# container, so a value of just the public hostname breaks every deploy.
ALLOWED_HOSTS=localhost,backend

# Database. Note the asymmetry: the backend reads POSTGRES_NAME and DB_HOST/DB_PORT,
# while the postgres image initialises itself from POSTGRES_DB. The compose file
# feeds POSTGRES_NAME to both, so set it once here.
POSTGRES_NAME=ciso
POSTGRES_USER=ciso
POSTGRES_PASSWORD=change-me-a-real-secret-goes-in-coolify
